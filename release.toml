allow-branch = ["main", "master"]
consolidate-commits = true

# Publish packages one at a time
publish = false

# Make sure sshash depends on sshash-lib being published first
dependent-version = "upgrade"
